# 2026-07-20

- Still no new founder input on the open items from the last two entries
  (packaging pick, price sign-off, sourcing supplier, trademark search).
  Rather than re-ask the same things again, focused today on turning the
  vague "legal formation" phase into an actual executable sequence, and
  found a genuinely new blocker in the process.
- **New doc**: `LEGAL_CHECKLIST.md` — the real Florida LLC formation
  sequence (Sunbiz name check → registered agent → Articles of
  Organization ($125) → EIN (free, direct from IRS) → business tax
  receipt → home occupation permit), so this phase is ready to execute
  once the founder gives the go-ahead, not still a research task.
- **Important finding**: the Local Business Tax Receipt isn't a single
  statewide thing — it's issued at the county level (and city level too,
  if inside city limits). The existing $50–150 estimate in
  `BUSINESS_PLAN.md` was a rough range across all Florida counties, not a
  real number. Need to know which county/city the business is actually
  based in to get a real figure.
- **New risk found**: most FL cities/counties require a separate *home
  occupation permit* for a home-based business, on top of cottage food
  law — and specifics vary by city (e.g. Tampa restricts storing/selling
  merchandise from the property). Not yet checked for the founder's actual
  location. Same blocker as the tax receipt: needs the county/city.
- Tried to check "King & Sepal LLC" name availability at Sunbiz directly —
  blocked with the same HTTP 403 pattern as the USPTO trademark search.
  Confirmed (not just assumed) this is a systemic limitation: both the
  state name registry and federal trademark registry need a human to
  actually search them.
- Updated `OPEN_QUESTIONS.md` (new Legal/entity section — county/city,
  registered agent, Sunbiz name check), `BUSINESS_PLAN.md` (linked the
  checklist, added the home occupation permit budget line), `RESEARCH.md`,
  and `README.md`.
- Did not spend money, file anything, send any outreach, or touch the
  Shopify store (didn't retry it today — no reason to expect the connector
  status changed since yesterday's block, and the founder's county/city is
  a higher-leverage single answer than another Shopify retry).

## Next
- **Single most useful answer the founder could give right now**: which
  Florida county (and city, if applicable) is the business based in? It
  unblocks both the Business Tax Receipt and the home occupation permit
  research in one shot.
- Still waiting on: packaging direction, price point sign-off, which
  supplier gets the sourcing email, registered agent choice, and the
  manual USPTO + Sunbiz name searches (a human needs to run both).
