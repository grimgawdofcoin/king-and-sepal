# 2026-07-24

- Tried to ask the founder directly (via AskUserQuestion) about four
  decisions that have accumulated: insurance now-vs-defer, registered
  agent choice, how to handle the paused supplier outreach, and whether to
  proceed with Shopify. **The question tool itself was rejected** — this
  automated run is non-interactive, so there's no one to answer live.
  Logged all four clearly in `OPEN_QUESTIONS.md` under a new "Pending
  founder decisions" section instead of leaving them scattered, so
  they're easy to answer whenever the founder is next in an interactive
  session.
- With Shopify, outreach, and live Q&A all unavailable this session,
  focused on two independent, non-blocked pieces of work:
  - **`RECIPE_TEST_PROTOCOL.md`** (new): a concrete, runnable kitchen-test
    plan — dissolve test with pass/fail criteria, eat-side texture check,
    per-flavor risk check (Hazelnut's fat content), and a shipping-heat
    stability check. The recipe has been paper-only since 2026-07-18; this
    turns "needs testing" into something actually actionable rather than a
    permanent caveat repeated in every entry.
  - **`docs/marketing/drop-strategy.md`** (new): validated the Ffern-style
    drop/scarcity model works well beyond fragrance (SKIMS, Supreme, Nike
    SNKRS, Glossier all use variants), and researched practical waitlist
    tooling (LaunchList, Waitlister, Klaviyo) for whenever Shopify setup
    happens. Recommended starting simple (an embedded signup form) rather
    than a dedicated drop tool, given the scale of a first drop.
- Updated `OPEN_QUESTIONS.md`, `RESEARCH.md`, and `README.md`.
- Did not spend money, file anything, send any outreach, or touch Shopify.

## Next
- The four pending decisions in `OPEN_QUESTIONS.md` need a real answer
  from the founder in an interactive session — this automated run can't
  get them.
- Whoever's available should try running Test 1 from
  `RECIPE_TEST_PROTOCOL.md` (the dissolve test) — it's the single highest-
  value validation step for the whole product concept and doesn't need any
  of the blocked tools.
